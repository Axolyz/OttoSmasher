# Demo 0.7 验证（2026-09-13）

本轮只重建派生节奏索引，没有重跑分离／音素模型，也没有宣称新模型精度提升。

## 覆盖范围

| 后端 | 固定目标句 | 可用整句节奏 | 额外语段 | 量化模式（含四倍率） |
|---|---:|---:|---:|---:|
| SOFA | 200 | 200 | 55 | 3060 |
| narabas | 200 | 200 | 150 | 4200 |
| HubertFA | 200 | 200 | 130 | 3960 |
| MFA | 200 | 184 | 74 | 2064 |

SOFA 27 句可拆，narabas 67 句、HubertFA 57 句、MFA 35 句可拆。
MFA 的 16 个不可用目标保留失败记录（5 个上游失败、11 个没有有效递增节奏）；MFA 的
可靠 mora 对应缺失，索引仅有校准／声学两路线，不伪造纯 mora 模式。
报告：`outputs/validation/rhythm-index-v1.json`，额外语段连接模式另存于索引中。

## 性能

同机，SOFA、120 BPM、四音块 `[0,.5),[1,1.5),[2,2.5),[3,3.5)`，容差 .12 拍，返回30条。
旧原录音匹配的历史热测约 8.807 秒、9189 次线性优化；算法语义不同，不比较返回数量为质量。

| 新路径 | 范围数 | 热测 |
|---|---:|---:|
| 仅整句、保持休止 | 200 | 531–541 ms |
| 整句＋语段、保持休止 | 255 | 555–564 ms |
| 整句＋语段、允许连接休止变化 | 255 | 1732–1766 ms |

上述包含匹配、返回30条方案的实体化和落盘；不含模型／索引首次构建。
新进程首次读已经构建的索引约1秒。SOFA 完整首次索引构建约13秒。
最终服务复测：`outputs/validation/index-search-performance-final.json`；先前记录保留在
`outputs/validation/index-search-performance-v1.json`。

## 声音与交互

- 实际渲染55个 SOFA 语段：WAV 帧长与所选源范围一致，误差0帧；没有借用其他模型或原混音。
- SOFA 55段与 narabas 150段切口未跨非静音模型音素；局部 phone/mora 索引和父范围检查通过。
  这项检查依据模型标记，不能证明其感知边界完全正确。
- 3个代表父句的5个语段，共34个严格音频保护核心，与所选原声样本逐样本一致。
  REAPER `copy=False` 导出的持久 WAV SHA 等于未变速语段，标记与试听坐标在1帧内一致，
  snap offset 正确；本轮没有重新操作真实 REAPER 剪贴板／保存工程。
- 三段句 `e3122e7e231d874faeb4329c`（`ほえ… / あ… う… あ…`）连接休止3/5格，
  7个核心（包含2个尾音控制点）和全部标记检查通过。
- Chrome UI 实测整句／三语段切换与快速切换、源声播放、来源编号、只读局部占格、
  保留父字幕上下文；两音查询命中语段1 ×2，显示0/.5拍并沿用命中 plan_id。
- 严格命中播放原先附加查询画布尾部静音，已修正：该语段现在0.643秒，界面估计0.64秒。
- 修改 BPM 后解除原命中绑定；连接休止1→2格使 `[0,.5,1.5,4] → [0,.5,2,4.5]`，
  段内时值不变。新方案不是把同一音频换一个标签。

最终 8765 服务另实测一个搜索命中语段的严格播放、WAV获取和完整导出，全部使用同一 plan_id，
记录为 `outputs/validation/demo07-live-handoff.json`。

实音频报告：`.runtime/logs/scope-validation-v1.json`、
`.runtime/logs/scope-strict-reaper-validation-v1.json`。试听功能与映射已验，最终素材质量由用户听感决定。

## 回归

最终完整回归 **138 个测试通过**，Ruff、JavaScript 语法和差异空白检查通过。

测试覆盖索引热查询不调用推断／波形、原整句映射不变、首尾与音块硬排除、固定四倍率、
弱辅助与无 mora 独立性、作用域失效、查询结果与计划一致、长休止不截断、局部句尾保持、
有界连接休止、多段位移、严格核心和导出来源。运行 `python -m pytest -q` 与 Ruff。
旧有连续线性匹配测试显式使用旧引擎，避免把旧的弹性停顿默认值误当作新索引要求。
