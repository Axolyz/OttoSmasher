# 音MAD 素材系统——可选概念架构参考

文档日期：2026-09-11

## 使用限制


本文件中的内容都是可推翻的概念建议。实现者可以重新选择技术栈、存储方式、播放器、搜索方案、界面形式和进程边界，不需要兼容任何旧代码或旧数据。

## 1. 建议把项目理解为一组协作工具

这个项目不适合做成一个承担全部功能的超级应用。更自然的划分是若干可以独立启动、通过稳定数据约定协作的工具：

1. 素材登记与台词检索工具。
2. 带画面和波形的播放器／截取器。
3. 独立采样管理器。
4. 分离、降噪、分析和拉平等处理工具。
5. 面向 DAW 与视频剪辑软件的交接能力。

这些是职责边界，不代表必须拆成五个程序、五个仓库或五个进程。新架构应根据复杂度决定物理拆分方式。

## 2. 三种入口汇入同一条后续流程

系统至少有三种素材入口：

- 带字幕片源：从台词搜索进入播放和截取。
- 无字幕片源：直接从播放器进入人工截取。
- 外部采样：绕过片源检索，直接手工导入采样库。

它们进入采样库以后，应尽量共享标签、试听、版本管理、模型处理和 DAW 交接能力。

字幕不应成为采样存在的前提。外部采样也不应被迫伪造片源或时间码。

## 3. 建议区分四类业务事实

为了避免搜索、截取和处理互相缠绕，可以在概念上区分：

### 3.1 原始素材

用户拥有的视频或音频，以及可选的字幕。它是来源事实，不因创建采样而改变。

### 3.2 临时备选素材

它可能只是待试听的想法，不一定值得进入正式采样库。

### 3.3 正式采样

经过人工确认、值得长期保存和检索的素材。它拥有标题、标签、评分、备注和来源信息。

### 3.4 处理版本

从某个采样版本经过分离、降噪、背景提取或音高拉平得到的新媒体文件。

这四类概念的名称可以改变，但它们表达的事实不宜混为一谈。例如，模型输出不应覆盖原始采样；临时区也不应自动污染长期采样库。

## 4. 模块间应交换事实，而不是互相控制界面

工具之间最重要的协作信息包括：

- 来源素材的稳定标识或路径。
- 时间范围。
- 可选的字幕关联和上下文。
- 采样或处理版本的稳定标识。
- 实际媒体文件位置。
- 标签、备注和人工评价。
- 处理来源、模型、参数和父版本。

播放器不需要知道采样库如何实现搜索；采样库也不需要操纵播放器内部控件。二者只需要能够围绕“打开某个素材的某个时间范围”和“保存当前选区”交换信息。

新架构可以采用本地命令、进程间通信、本地服务、共享数据层或其他方式，但应优先保证接口简单、可调试和可替换。

## 5. 播放与截取是同一个视觉工作流

对于较长视频，单独看波形不足以可靠定位素材。因此截取工具应把以下内容同步呈现：

- 原视频画面。
- 音频波形。
- 当前时间和选区。
- 当前字幕及附近上下文（如果存在）。

带字幕搜索的结果应能直接打开到对应位置；无字幕视频也应使用同一套 IN、OUT、循环试听和导出能力。

未来若加入日语音素级时间戳，可以在这个工作流中增加按音素吸附、选择或微调，但当前不需要实现。

## 6. 采样管理器应保持独立

采样管理器是制作阶段的常驻工具，不应要求同时打开字幕搜索器或完整片源浏览器。

它的中心不是目录树，而是：

- 快速试听。
- 标签组合筛选。
- 标题、评分和备注。
- 多版本比较。
- 来源追溯。
- DAW 和视频剪辑软件交接。

文件夹可以作为物理存放手段，但不应承担主要分类语义。交给 DAW 的实际文件仍应具有容易辨认的文件名。

## 7. 处理链应采用非破坏式版本关系

每次处理都应产生新版本，并记录它来自哪个输入版本。例如：

```text
原始截取
  ├─ 人声分离结果 A
  │    ├─ 降噪结果
  │    └─ 音高拉平结果
  ├─ 人声分离结果 B
  └─ 背景或非人声结果
```

同一种处理可以有多个模型或参数结果并存。版本类型只是用途描述，不应被当成唯一槽位。

人工可以选择偏好版本，但系统不应自动宣称某个模型结果是最佳版本。

## 8. 模型处理应与交互界面解耦

分离、降噪和后续分析通常比搜索、试听和编辑更慢，也更容易因模型、显存或环境问题失败。

因此，概念上应把一次处理看成可观察的任务：

- 明确输入和目标处理。
- 明确模型、参数和运行设备。
- 报告进行中、成功或失败。
- 成功后登记实际输出。
- 失败时不产生伪结果，也不改变原始文件。
- 可以安全重试。

分离和降噪当前优先围绕 pymss 的模型能力设计，但业务数据不应只能被某个特定模型或模型版本解释。

## 9. GUI 与 CLI 的建议边界

适合 GUI 的工作：

- 看视频和波形。
- 调整选区。
- 循环试听。
- 浏览字幕上下文。
- 快速试听采样。
- 编辑标签和比较版本。
- 向外部创作软件拖放素材。

适合 CLI 或后台任务的工作：

- 导入和批量登记。
- 建立或更新搜索数据。
- 批量媒体转换。
- 运行模型处理。
- 重建缓存。
- 环境诊断。
- 自动化测试。

这只是交互原则，不要求所有非视觉功能都只能使用 CLI。关键是避免为低频维护操作制造大量 GUI 状态和业务分支。

## 10. 数据与媒体文件应分开考虑

新架构需要分别回答两个问题：

1. 标签、来源、时间范围、字幕关系和处理历史存在哪里？
2. 原片、截取文件、处理结果、波形和缩略图存在哪里？

无论选择什么数据库或文件布局，都应满足：

- 原片通常可以保留在用户原位置。
- 正式采样和处理结果有清晰的生命周期。
- 缓存可以重建，不应与唯一原件混淆。
- 外部手工采样可以明确选择链接原文件或纳入管理。
- DAW 获得的是普通、稳定、可读取的真实文件。
- 数据库内部标识不应迫使 DAW 显示不可读文件名。

具体采用数据库、搜索引擎、清单文件或混合方案，应由新 session 重新论证。

## 11. 搜索与播放之间的最小协作

台词检索结果至少需要提供：

- 对应素材。
- 字幕文本。
- 起止时间。
- 前后字幕上下文。

播放器至少需要接受“素材加时间位置或时间范围”，并允许用户从该位置继续浏览、修改边界和保存选区。

日语搜索如何实现，不应由播放器决定。搜索后端可以逐步从可靠的原文和归一化搜索扩展到读音、词形和分词检索。

## 12. DAW 与视频剪辑交接必须真实验证

架构图中存在“拖放”按钮并不代表桌面软件一定接受它。新方案应尽早使用用户真实采用的 DAW 和视频剪辑软件做小型验证。

最低回退能力应包括：

- 获得实际文件路径。
- 在文件管理器中定位文件。
- 生成具有可读名称的交接文件。
- 对视频素材提供短片文件，或提供原片和明确时间范围。

最终保留哪几种方式，应根据真实软件行为决定。

## 13. 建议的实施顺序

第一阶段先验证最短闭环：

```text
真实片源与字幕
→ 搜索台词
→ 带画面和波形地定位
→ 人工截取
→ 加入独立采样库
→ 标签检索与试听
→ 交给真实 DAW
```

随后再加入一个真实的 pymss 处理闭环，确认多版本、错误保护和 A/B 试听成立。

音高拉平、批处理、日语高级搜索和灵感辅助分析可以在基础闭环稳定后分别推进，避免它们互相阻塞。

## 14. 最容易造成返工的决策

新 session 在编码前应特别验证：

- 播放器方案能否同时满足画面、波形、字幕、精细定位和外部控制。
- 真实 DAW 是否接受所设计的拖放方式。
- 处理版本能否允许同类型多结果并存。
- 外部手工采样的文件所有权如何定义。
- 长视频的波形、缩略图和随机跳转是否足够流畅。
- Windows 下 Unicode 路径、外部进程和 GPU 环境是否可靠。
- pymss 不同模型的输出 stem 如何明确展示，而不是靠文件名猜测。
- 音高拉平是否有真实可用的质量，而不只是检测出 F0。

## 15. 新 session 拥有完全设计自由

新 session 不需要兼容任何旧代码、旧目录、旧数据库、旧命令或旧界面，也不应寻找它们。

它唯一需要继承的是需求文档表达的用户工作流和约束。本文件只是当独立设计遗漏某些边界时使用的检查清单。
