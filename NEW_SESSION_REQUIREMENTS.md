# 音MAD 素材检索、截取与采样管理系统——需求文档


## 给新 Codex session 的使用说明

在用户确认新方案之前，不要开始大规模编码。可以先检查测试素材、运行环境，并广泛调研可复用的开源项目。用户接受魔改和“积木式”组合，不要求从头造轮子。

`OPTIONAL_ARCHITECTURE_REFERENCE.md`是旧架构的参考。该文档只记录概念层面的备选思路，不包含旧源码结构，也不是新方案必须继承的设计。

## 1. 项目定位

这是一个仅供用户本人使用的音MAD素材处理系统，主要面对带日语字幕的动画、普通视频、无字幕视频，以及用户自行截取或从其他来源获得的音频采样。

它的目的不是自动生成音MAD，而是减少以下重复劳动：

- 从大量动画和视频中寻找合适台词。
- 结合画面、字幕和声音快速判断素材是否可用。
- 精确截取台词、人声、撞击声和其他音效。
- 对采样执行人声分离、降噪等处理。
- 保存原始版和各种处理版之间的关系。
- 通过标签、试听和评分重新找到采样。
- 快速把选中的素材送入 DAW 或视频剪辑软件。

系统必须坚持 Human-in-the-loop：音色、语气、情绪、节奏感和实际可用性最终由人试听和判断。自动分析只能帮助缩小范围、完成处理和整理资产，不能替代审美裁决。

## 2. 目标用户与使用假设

唯一目标用户就是项目所有者本人。

用户特点：

- 是极客用户，能理解项目结构和数据流。
- 能使用命令行，不要求大众化安装向导和新手引导。
- 更看重工作流速度、透明度和可控性，而不是界面包装。
- 接受多个独立工具协作，不希望所有功能塞进一个超级工作站。
- 接受使用和魔改开源项目；项目不会公开，也不用于商业用途。

## 3. 核心原则

### 3.1 人工决策优先

系统可以推荐、排序、分析或预处理，但不得自动替用户决定哪些台词和采样值得保留。

### 3.2 非破坏式处理

原始素材和原始采样必须保留。分离、降噪、拉平等处理结果应作为独立版本存在，不得静默覆盖原文件。

### 3.3 来源可追溯

只要采样来自某个片源，就应能知道：

- 来源文件是什么。
- 对应的起止时间是什么。
- 如果有字幕，对应哪一句或哪几句字幕。
- 它经过了哪些处理。
- 当前试听或导出的究竟是哪一个版本。

### 3.4 有字幕与无字幕流程兼容

字幕是重要入口，但不能成为截取功能的前提。无字幕动画、普通视频、音频文件和手工采样都必须能够进入截取与采样管理流程。

### 3.5 功能解耦

至少应允许以下工作独立进行：

- 台词或片源检索与浏览。
- 片源播放和采样截取。
- 采样库检索、试听与导出。
- 模型处理或批处理。

例如，用户制作音MAD时应能只启动采样管理器，而不用同时运行字幕搜索和片源播放器。

### 3.6 只在必要处使用 GUI

需要看画面、看波形、拖动切点、试听、比较版本和拖放文件的地方适合 GUI。导入、批处理、模型运行、重建索引、导出和诊断可以优先提供快捷、可组合的 CLI。

## 4. 主要业务场景

### 4.1 Rap 台词制作

用户输入日语文字，找到包含相关台词的字幕结果，试听原声并查看对应画面和上下文。用户根据语气、情绪和节奏人工选择片段，调整起止点，保存为候选或正式采样，然后拖入 DAW 或视频剪辑软件进行卡点拼接。

### 4.2 调音用人声采样

用户从台词或其他片段中截取较稳定、较长的人声音素或元音。必要时先做人声分离或降噪，再进行音高轨迹拉平，以便后续在音MAD工程中使用。

这里的需求是“拉平音高轨迹”，不是提供普通的整体升降调工具。默认目标应是尽量保持原采样具有代表性的音高，只消除音高起伏。若实现者认为这个定义仍不足以决定算法，应先用试听原型与用户确认，不能擅自扩展成通用变调器。

### 4.3 鼓组和背景音采样

用户从动画中截取撞击、脚步、关门、爆炸、摩擦、呼吸、短促噪声等声音，作为底鼓、军鼓、打击乐或特殊音效。

这些采样可能恰恰需要保留背景声，而不是提取人声，因此必须允许保存和管理不同处理方向的版本。

### 4.4 无字幕素材截取

用户打开没有字幕的视频或音频，结合画面和波形定位，设置起止点并保存采样。这个流程不能依赖台词记录。

### 4.5 手工采样导入

用户可能在系统外使用其他工具截取或处理采样。采样管理器必须允许直接导入这些文件，并补充标题、标签、来源备注和版本关系。

### 4.6 浏览原片寻找灵感

用户有时并没有明确搜索词，只想快速翻看动画寻找有趣片段。理想状态下，系统能降低长时间空白、只有背景音乐或低信息量片段的干扰，并帮助发现：

- 台词密集片段。
- 情绪剧烈变化。
- 尖叫、笑声、哭声、喘息等特殊人声。
- 撞击、爆炸和其他明显瞬态。
- 动作密集或画面变化明显的片段。

这属于重要的后续能力，但不应阻塞最初的字幕搜索、人工截取和采样库闭环。

## 5. 功能需求

### 5.1 片源管理

系统应支持登记本地视频或音频文件，并读取制作流程需要的基本信息，例如时长、音视频轨道和字幕关联。

要求：

- 首要支持常见动画片源格式，包括 MKV 和 MP4。
- 首要支持外部 SRT 日语字幕。
- 应考虑一个片源有多个音轨或字幕轨的情况。
- 不要求把原片复制进系统内部；允许保留原片路径并记录来源。
- 不需要实现复杂的路径迁移、目录修复或文件夹状态同步功能。
- 用户可以明确选择要使用的音轨和字幕轨。
- 文件名和路径可能包含空格、日文、中文和特殊符号。

### 5.2 日语字幕与台词检索

基本要求：

- 导入片源和对应字幕后，可以按台词文字搜索。
- 搜索结果必须保留片源、时间范围和字幕文本。
- 点击结果可以立即试听对应声音并观看对应画面。
- 可以跳到结果前后查看语境，而不是只播放被字幕框住的一小段。
- 搜索结果应能显示同一位置附近的前后字幕。
- 可以从搜索结果直接进入精确截取流程。
- 再次打开来自字幕语料库的素材时，仍能看到相关字幕。

日语检索体验应逐步支持：

- 原文子串搜索。
- 全角/半角、空白、常见标点的归一化。
- 平假名、片假名或读音层面的辅助搜索。
- 词形变化和日语分词搜索。
- 说话人或角色过滤。

第一版可以先保证可靠的原文和归一化搜索，但不能把简单英文式分词当成最终日语搜索方案。

### 5.3 片源播放与精确截取

这是最需要视觉交互的部分。

必须提供：

- 视频画面和音频同步播放。
- 可缩放或至少足够清晰的波形。
- 当前播放位置。
- 快速前后跳转。
- 逐帧或足够精细的定位能力。
- 有字幕时显示当前字幕和附近字幕。
- 无字幕时仍完整可用。
- 一键把选区保存为候选片段。
- 可以导出音频片段、视频片段或两者。

如果导入的是一分钟以上甚至整集动画，不能只给音频波形而没有参考画面。用户明确需要结合画面定位。


### 5.4 候选片段与正式采样

需要区分“正在考虑的原片范围”和“已经确认值得进入采样库的素材”。

候选片段应支持：

- 来源片源和起止时间。
- 音频、视频或两者。
- 可选的字幕关联。
- 备注。
- 接受、拒绝或保留待定。

正式采样应支持：

- 标题。
- 来源和时间范围。
- 对应字幕及上下文入口。
- 标签。
- 评分或实用性等级。
- 备注。
- 多个处理版本。
- 参考视频或返回原片播放位置的入口。

### 5.5 独立采样管理器

采样管理器是制作时最常驻的工具之一，必须可以独立启动。

核心交互：

- 点击采样立即发声。
- 可选择试听不同版本。
- 能快速停止、重复播放或切换到下一个采样。
- 按文字和标签搜索。
- 多标签组合过滤。
- 按角色、说话人、用途、情绪、音色、来源作品、处理状态等维度筛选。
- 可以编辑标题、标签、评分和备注。
- 可以手工导入外部采样。
- 可以看到采样来自哪里。
- 可以跳回原片对应位置。
- 可以找到或生成对应参考视频。
- 可以把当前选择的版本交给 DAW。
- 如有对应视频片段，也应能交给 AE 或其他剪辑软件。

采样组织必须以标签为中心，而不是以目录层级为中心。用户已经接受不再依赖文件夹浏览来管理采样。

### 5.6 DAW 与视频剪辑软件交接

DAW handoff 是核心需求，不是附属功能。

最低要求：

- 从采样管理器选择一个版本后，可以快速得到真实音频文件。
- 应优先支持直接拖入 DAW；若目标软件限制拖放，至少要有可靠的路径复制或等价快捷方式。
- DAW 中显示的文件名应具备辨识度，至少能看出采样标题或来源含义，不能只是一串不可读 ID。
- 用户无需通过文件夹管理素材才能完成工作。
- 导出的文件必须是真实、稳定、可被 DAW 读取的普通媒体文件。

视频交接最低要求：

- 能获得对应原片时间范围的视频片段，或让剪辑软件方便地定位原片和时间范围。
- 用户希望能尽快把参考画面或原片段拖入 AE 开始剪辑。

需要在真实 DAW 和 AE 工作流中验证拖放行为，不能只在浏览器里假设 `file://` 拖拽一定可行。

### 5.7 标签、评分和组织

系统不应强制固定标签体系，但应适合使用命名空间式标签，例如：

- `character:サーバル`
- `speaker:角色名`
- `work:作品名`
- `use:rap`
- `use:kick`
- `use:snare`
- `voice:scream`
- `emotion:angry`
- `quality:versatile`
- `source:manual`
- `process:separated`

要求：

- 标签可随时增删。
- 支持自动补全或快速复用已有标签。
- 支持按多个标签求交集。
- 可以表示采样的泛用程度和主观价值。
- 标签不应替代来源、时间范围和处理历史等结构化事实。

### 5.8 采样版本管理

一个采样可以拥有多个版本，例如：

- `raw`：原始截取。
- `vocals` 或 `speech`：人声提取。
- `denoised`：降噪版本。
- `background`：背景或非人声版本。
- `impact`：面向瞬态或撞击用途的版本。
- `flattened`：音高轨迹拉平版本。
- 其他用户自定义版本。

要求：

- 原始版必须保留。
- 每个处理版应知道自己基于哪个版本生成。
- 应记录处理工具、模型和关键参数。
- 不得静默覆盖已有版本。
- 允许同一种处理使用多个模型或参数做对比；不能因为名字相同就限制每个采样只能存在一个结果。
- 用户可以试听比较后，指定默认或偏好的版本。
- 模型结果不会自动成为“最佳版本”。

### 5.9 分离与降噪

用户已经明确：分离和降噪统一优先使用 pymss 支持的各种模型，不再要求接入 DeepFilterNet。

要求：

- 支持对单个采样运行。
- 后续支持批量处理选中的采样。
- 支持选择 pymss 模型和输出 stem。
- 可以保存人声、非人声、背景或模型提供的其他输出。
- 处理前后可以方便地 A/B 试听。
- 处理失败不能污染现有采样。
- 模型下载、模型缓存和运行环境应可诊断。
- 应充分利用本机 NVIDIA GPU，但要能明确提示显存不足、CUDA 不可用或回退 CPU 的情况。
- 不应假设所有模型都使用相同的 stem 名称。
- 模型输出必须由用户试听判断是否适合 rap、调音或鼓组用途。

### 5.10 音高轨迹分析与拉平

用户只需要音高拉平，不需要普通变调功能。

要求：

- 能分析适合调音的人声采样的 F0 轨迹。
- 可以识别无声、清辅音和不可靠 F0 区域，避免把所有帧强行视为有音高。
- 拉平结果应尽量保留时长、音色、辅音边缘和瞬态。
- 默认不应额外改变整段采样的中心音高。
- 结果必须作为新版本保存。
- 必须能够和原始版、分离版、降噪版进行试听比较。
- 如果当前没有可靠后端，应明确标记为未实现，不能生成质量不可控但命名为 `flattened` 的伪结果。

具体模型和算法由新 session 调研并提出，但需要通过真实动漫人声试听，而不是只通过合成正弦波测试。

### 5.11 自动分析与灵感辅助

可自动生成用于筛选的辅助信息：

- 静音或低信息量区间。
- 台词活动区间。
- 瞬态候选。
- 尖叫、笑声、哭声、喘息等人声事件候选。
- 画面变化或动作密度候选。
- 背景音乐占主导的片段。

### 5.12 任务与批处理

模型处理和长媒体分析可能耗时较长，应能够看到：

- 正在处理什么。
- 使用哪个模型或处理方式。
- 当前状态。
- 成功输出。
- 失败原因。
- 是否可安全重试。

后续应支持任务取消、失败重试和批量运行。第一版至少不能让失败静默消失。

## 6. 明确不需要或暂缓的内容

以下内容不是当前优先事项：

- 面向普通用户的复杂安装向导。
- 多用户、账户、权限和在线协作。
- 云端同步。
- 商业发布和许可证治理界面。
- 自动修复用户移动过的所有文件路径。
- 通过移动文件夹来修改素材状态。
- 强制依赖文件夹层级组织采样。
- 全自动判断“最好听”或“一定能用”的采样。
- 自动生成完整音MAD。
- 按日语音素精确切割；当前只要求为未来扩展预留概念空间，不在第一阶段实现。
- 普通整体变调功能。
- DeepFilterNet 接入。

## 7. 非功能需求

### 7.1 平台

主要运行平台是 Windows。当前测试机器有 NVIDIA RTX 3060 Laptop GPU，显存 6GB。

### 7.2 稳定性

- 所有媒体处理失败都应给出可理解的错误。
- 不应因某个模型失败而破坏原始文件或采样库。
- 不应静默覆盖已有导出。
- 应正确处理日文和中文路径。
- 长视频操作不能要求一次性把完整媒体加载进内存。

### 7.3 性能

- 台词搜索应接近即时响应。
- 点击采样后的试听应快速开始。
- 长片源的波形和缩略图可以缓存。
- 重复处理相同输入和相同参数时，应考虑复用已有结果。
- 深度学习任务应优先利用 GPU，同时允许明确的 CPU 模式。

### 7.4 可观察性

由于用户能使用命令行，应提供足够日志和诊断信息，包括外部工具版本、模型名称、设备选择、输入输出路径和失败栈的有效摘要。

### 7.5 可替换性

模型和外部工具会持续变化。业务数据不应和某一个特定模型不可分割地绑定。更换模型后，已有原始采样、标签、来源和人工评价仍应可用。

## 8. 优先级建议

以下是业务优先级，而不是技术实施方案。

### P0：必须先形成的真实闭环

1. 使用真实 MKV 与 SRT 导入片源。
2. 正确搜索日语字幕。
3. 点击结果查看画面并试听。
4. 用波形和画面调整选区。
5. 保存候选并接受为采样。
6. 在独立采样管理器中按标签找到并试听采样。
7. 把真实音频文件交给 DAW。
8. 对同一采样运行至少一个 pymss 人声模型，并把正确 stem 保存为新版本。

### P1：进入日常制作

1. 完善采样标签、评分和编辑。
2. 可靠的原片跳转和参考视频。
3. 多个 pymss 模型或处理版本的 A/B 比较。
4. 批量分离或降噪。
5. 真实 DAW 和 AE 拖放验证。
6. GPU 推理环境和显存不足处理。
7. 高质量 F0 分析与音高拉平原型。

### P2：提高发现效率

1. 更好的日语分词、读音和词形搜索。
2. 自动说话人或角色辅助标注。
3. 瞬态、特殊人声和情绪候选。
4. 跳过低信息量片段的灵感浏览模式。
5. 更成熟的任务队列、缓存和批处理。

## 9. P0 验收场景

### 场景 A：字幕台词到 DAW

给定一集动画及其日语 SRT：

1. 导入后能搜索 `サーバル`。
2. 搜索结果显示正确日文，而不是乱码。
3. 点击一条结果能播放对应时间附近的画面和声音。
4. 用户调整起止点并循环试听。
5. 用户保存为正式采样并添加 `character:サーバル`、`use:rap` 等标签。
6. 独立打开采样管理器后能通过标签找到它。
7. 点击立即试听。
8. 拖入或交给 DAW 后得到可识别名称的真实 WAV 文件。

### 场景 B：无字幕视频截取

给定一个没有字幕的视频：

1. 能播放视频并显示波形。
2. 能设置和调整选区。
3. 能导出音频或视频。
4. 能作为采样加入同一个采样库。

### 场景 C：手工采样导入

给定一个用户在系统外制作的 WAV：

1. 可以直接导入采样管理器。
2. 可以添加标签、评分和备注。
3. 可以试听并交给 DAW。
4. 不要求它必须关联某个字幕或片源。

### 场景 D：pymss 处理

给定一个正式采样：

1. 用户选择 pymss 模型和目标 stem。
2. 系统运行处理并显示设备、模型和状态。
3. 正确保存目标 stem，而不是因文件名猜测错误保存另一个 stem。
4. 原始版不变。
5. 新版本可独立试听和交给 DAW。
6. 处理历史可追溯。

### 场景 E：失败保护

当模型缺失、CUDA 不可用、显存不足、FFmpeg 失败或输出文件不存在时：

1. 原始采样不变。
2. 不创建假成功版本。
3. 用户能看到失败原因。
4. 修复环境后可以安全重试。

## 10. 已提供的真实测试材料

项目目录中提供：

- `bangumi/[Kemono Friends][01].mkv`
- `bangumi/[Kemono Friends][01].srt`

这套材料可以用于验证日语字幕、HEVC 视频、FLAC 音轨、Unicode 文件名、真实切片和 pymss 处理。

## 11. 新 session 在架构前应向用户确认的问题

这些问题不会阻止先提出初步方案，但会影响日常工作流：

1. 用户主要使用哪一个 DAW，版本是什么？
2. 用户主要使用哪个 AE 版本，以及最希望的原片交接方式是什么？
3. 采样默认试听是否需要键盘驱动，例如上下选择、空格试听、单键加标签？
4. 同一处理类型是否需要保留多个模型结果并并排比较？本需求默认答案为“需要”。
5. 音高拉平的默认目标是否严格取原片中位 F0，还是允许用户在“拉平”时指定一个目标音名？用户最新表述是不需要普通改音高，因此不要擅自加入通用变调。
6. pymss 第一批固定预设需要覆盖哪些用途：对白人声、普通人声、去混响、去噪、背景、呼吸、撞击或其他？
7. 参考视频交给 AE 时，用户更偏好实际短视频文件，还是原片加时间码也足够？
